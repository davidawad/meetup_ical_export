# Copy this file to .env and fill in real values — .env itself is gitignored,
# this file (.env.example) is the tracked, secret-free record of every
# environment variable the project reads. Add a line here whenever code
# starts reading a new one; never let .env drift ahead of this file.
#
# EXAMPLE_API_KEY=
