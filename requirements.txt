# Runtime dependencies. Bumped from the 2019-era pins that shipped with the
# original REST version. `pytz` and `python-dateutil` are gone: the app targets
# Python 3.11+, so stdlib `zoneinfo` covers timezones and
# `datetime.fromisoformat` parses Meetup's ISO-8601 DateTime scalar. (icalendar
# still pulls python-dateutil in transitively; we no longer pin it ourselves.)
Flask==3.1.3
requests==2.34.2
icalendar==7.2.2
